# Keep the Reading Deck as an article-derived view

The Reading Deck remains an alternate view of the canonical article and its separately authored TLDR, using content already processed by Astro's ordered Markdown pipeline. It will not become an independently authored Reveal.js presentation or use a second Markdown parser, because doing so would duplicate content semantics, risk divergence in citations and marginalia, and make the deck compete with the article's canonical route.
